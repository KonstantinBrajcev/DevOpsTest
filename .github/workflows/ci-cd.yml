name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: ./app
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-django

      - name: Run tests
        working-directory: ./app
        env:
          DEBUG: "True"
          SECRET_KEY: test-secret-key
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          ALLOWED_HOSTS: localhost,127.0.0.1
        run: |
          cd src
          python manage.py test myapp --noinput

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Django app
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/django-app:latest
            ${{ secrets.DOCKER_USERNAME }}/django-app:${{ github.sha }}

      - name: Build and push Nginx
        uses: docker/build-push-action@v5
        with:
          context: ./nginx
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/django-nginx:latest
            ${{ secrets.DOCKER_USERNAME }}/django-nginx:${{ github.sha }}

      - name: Debug SSH Connection
        run: |
          echo "🔍 Диагностика SSH..."
          echo "HOST: ${{ secrets.DEPLOY_HOST }}"
          echo "USER: ${{ secrets.DEPLOY_USER }}"
          echo "PASSWORD: [скрыто]"

          # Проверяем доступность сервера
          timeout 5 nc -zv ${{ secrets.DEPLOY_HOST }} 22 && echo "✅ Порт 22 открыт" || echo "❌ Порт 22 закрыт"

          # Тест с sshpass
          echo "Тестируем sshpass..."
          sshpass -p "${{ secrets.DEPLOY_PASSWORD }}" ssh -v -o StrictHostKeyChecking=no \
            -o ConnectTimeout=5 \
            -o BatchMode=yes \
            ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "whoami && pwd" || echo "SSH тест не удался"

      - name: Deploy Django Step by Step
        if: success()
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_PASSWORD: ${{ secrets.DEPLOY_PASSWORD }}
        run: |
          # Установка
          sudo apt-get update && sudo apt-get install -y sshpass

          # Переменные
          TARGET="/var/www/user2142861/data/www/jelezo.by/DevOpsTest"
          PYTHON="/var/www/user2142861/data/.localpython/bin/python3"

          # Шаг 1: Тест подключения
          echo "🔗 Тест SSH..."
          sshpass -p "$DEPLOY_PASSWORD" ssh -o StrictHostKeyChecking=no \
            $DEPLOY_USER@$DEPLOY_HOST "echo '✅ Подключено как:' && whoami"

          # Шаг 2: Подготовка
          echo "📁 Подготовка..."
          sshpass -p "$DEPLOY_PASSWORD" ssh -o StrictHostKeyChecking=no \
            $DEPLOY_USER@$DEPLOY_HOST "mkdir -p '$TARGET' && cd '$TARGET' && ls -la"

          # Шаг 3: Копирование
          echo "📤 Копирование..."
          cd app
          tar -czf - . > /tmp/app.tar.gz
          sshpass -p "$DEPLOY_PASSWORD" scp -o StrictHostKeyChecking=no \
            /tmp/app.tar.gz $DEPLOY_USER@$DEPLOY_HOST:"$TARGET/"
          sshpass -p "$DEPLOY_PASSWORD" ssh -o StrictHostKeyChecking=no \
            $DEPLOY_USER@$DEPLOY_HOST "cd '$TARGET' && tar -xzf app.tar.gz && rm app.tar.gz"
          cd ..

          # Шаг 4: Установка
          echo "🐍 Установка Django..."
          sshpass -p "$DEPLOY_PASSWORD" ssh -o StrictHostKeyChecking=no \
            $DEPLOY_USER@$DEPLOY_HOST "cd '$TARGET' && '$PYTHON' -m pip install --user -r requirements.txt"

          # Шаг 5: Запуск
          echo "🚀 Запуск..."
          sshpass -p "$DEPLOY_PASSWORD" ssh -o StrictHostKeyChecking=no \
            $DEPLOY_USER@$DEPLOY_HOST "\
            cd '$TARGET'/src && \
            '$PYTHON' manage.py migrate && \
            '$PYTHON' manage.py collectstatic --noinput && \
            nohup '$PYTHON' manage.py runserver 0.0.0.0:8000 &
            "

          echo "✅ Готово! http://178.159.242.118:8000"
